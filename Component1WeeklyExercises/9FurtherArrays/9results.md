# 9 Further Arrays
15 marks  
**to be committed to GitHub by 20th April**

1 Histogram  -- complete
2 LL Bank Accounts   -- complete
3 Post Codes   -- complete 
4 Quiz Time -- complete

Please replace **todo** with **complete** when done.  
Record your progress on the OOP Form on README.md.

---

For each question in the exercise, please either display the output generated by running the program, or the answer if the task is a question.

1 -  
public static void main(String[] args) {
        Scanner scan = new Scanner(System.in);
        int[] ranges = new int[10];

        System.out.println("Enter integers (1-100). Enter 0 or out of range to finish:");

        while (scan.hasNextInt()) {
            int val = scan.nextInt();
            if (val < 1 || val > 100) break;
            
            // Logic: (val - 1) / 10 gives the correct index (0-9)
            ranges[(val - 1) / 10]++;
        }

        System.out.println("\nHistogram:");
        for (int i = 0; i < ranges.length; i++) {
            int lower = (i * 10) + 1;
            int upper = (i + 1) * 10;
            
            // Formatting the label
            String label = String.format("%2d - %-3d | ", lower, upper);
            System.out.print(label);

            // Print asterisks
            for (int j = 0; j < ranges[i]; j++) {
                System.out.print("*");
            }
            System.out.println();
        }
    }
(4 marks)  
---output : <img width="685" height="495" alt="image" src="https://github.com/user-attachments/assets/3370b1b5-62be-481d-be9d-d0db7cefcfca" />


2 -  
(4 marks)

---

3 -  
(4 marks)

---

4 -  
(3 marks)

---



